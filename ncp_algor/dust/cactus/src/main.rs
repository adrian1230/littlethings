// n = len of array
// d1 ... dn = vertex i with degree di
// find if there is any cactus?
// parallel edges and loops are prohibited

// further specific correpsonding constraints
// pls refer to another code file

mod array;

fn main() {
    let v: Vec<i32> = Vec::new();
    array::generate(v);
    // println!("{:?}",array::generate(v));
}
